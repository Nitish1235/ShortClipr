__pycache__
*.py[cod]
*$py.class
venv
.venv
.env
.env.*
.DS_Store
.pytest_cache
.mypy_cache
.coverage
htmlcov

# ── binaries/ ─────────────────────────────────────────────────────────────────
# Only the compiled binary (binaries/bgutil-pot) is COPYed into the image.
# The fetch helper script is host-only and must stay out of the build context.
binaries/fetch_bgutil.sh
